# Deploy

## GitHub Pages

1. Open repository settings.
2. Go to Pages.
3. Choose `Deploy from a branch`.
4. Select `main` and `/root`.
5. Save.

The animated Agentropolis homepage will serve from `index.html`.

## Local preview

Open `index.html` in a browser.

No build step required.
