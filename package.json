{
  "name": "agentropolis-agent-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Governed Cloudflare Worker and remote MCP capability membrane for the Agentropolis Intelligence Grid.",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "npx --yes wrangler@4.114.0 dev",
    "deploy": "npx --yes wrangler@4.114.0 deploy && npx --yes wrangler@4.114.0 d1 migrations apply DB --remote",
    "deploy:dry": "npx --yes wrangler@4.114.0 deploy --dry-run --outdir .wrangler/dry-run",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-deployment.mjs",
    "check": "npm run validate && npm test && npm run deploy:dry",
    "herdr:detect": "node integrations/herdr/cli.mjs detect",
    "herdr:context": "node integrations/herdr/cli.mjs context",
    "herdr:verify": "node integrations/herdr/cli.mjs verify-context",
    "herdr:canary": "node integrations/herdr/cli.mjs canary",
    "jspace:sync": "node scripts/sync-jspace-projection.mjs",
    "jspace:readiness": "node scripts/jspace-beta-readiness.mjs",
    "jspace:verify-production": "node scripts/verify-jspace-production.mjs",
    "db:migrate:local": "npx --yes wrangler@4.114.0 d1 migrations apply DB --local",
    "db:migrate:remote": "npx --yes wrangler@4.114.0 d1 migrations apply DB --remote"
  }
}
